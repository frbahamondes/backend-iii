# 🐶 AdoptMe - Backend API

Bienvenido a **AdoptMe**, una API RESTful que permite gestionar usuarios, mascotas y procesos de adopción. Esta aplicación está desarrollada con Node.js, Express, MongoDB y Docker.

---

## ✅ Funcionalidades principales

- Gestión de usuarios (`/api/users`)
- Gestión de mascotas (`/api/pets`)
- Subida de imágenes con `multer`
- Endpoint para adopción de mascotas (`/api/adoptions`)
- Autenticación con JWT (`/api/sessions`)
- Generación de datos mock (`/api/mocks`)
- Documentación con Swagger
- Dockerización completa
- Tests funcionales con Supertest

---

## 🧪 Tests funcionales (`adoption.router.js`)

Para correr los tests:

```bash
npm install
npm test
